---
apiVersion: v1
kind: Service
metadata:
  name: civilian-svc
  namespace: {{ include "agd.namespace" . }}
  labels:
    {{- include "agd.labels" . | nindent 4 }}
    app.kubernetes.io/name: civilian-svc
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.civilianSvc.port }}
      targetPort: 8000
      name: http
  selector:
    app.kubernetes.io/name: civilian-svc
    app.kubernetes.io/instance: {{ .Release.Name }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: civilian-svc
  namespace: {{ include "agd.namespace" . }}
  labels:
    {{- include "agd.labels" . | nindent 4 }}
    app.kubernetes.io/name: civilian-svc
spec:
  replicas: {{ .Values.civilianSvc.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: civilian-svc
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: civilian-svc
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: civilian-svc
          image: {{ include "agd.image" (dict "img" .Values.civilianSvc.image "context" .) }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8000
          env:
            - name: PORT
              value: "8000"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: agd-secrets
                  key: db-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: agd-secrets
                  key: jwt-secret
            - name: DATABASE_URL
              value: "postgresql://{{ .Values.postgres.username }}:$(DB_PASSWORD)@postgres:5432/{{ .Values.postgres.database }}"
          envFrom:
            - configMapRef:
                name: agd-config
          resources:
            {{- toYaml .Values.civilianSvc.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
